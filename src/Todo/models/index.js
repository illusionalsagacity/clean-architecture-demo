import * as TodoModel from "./TodoModel";
import * as TodoCollection from "./TodoCollection";
import * as TodoListModel from "./TodoListModel";
import * as TodoListCollection from "./TodoListCollection";

const models = {
  TodoModel,
  TodoCollection,
  TodoListModel,
  TodoListCollection,
};

export default models;
